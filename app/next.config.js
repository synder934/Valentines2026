/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',      // enables static HTML export
  trailingSlash: true,   // optional for GitHub Pages routing
};

module.exports = nextConfig;
